import { TestBed } from '@angular/core/testing';

import { VowCommonFunctionService } from './vow-common-function.service';

describe('VowCommonFunctionService', () => {
  let service: VowCommonFunctionService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(VowCommonFunctionService);
  });

  // it('should be created', () => {
  //   expect(service).toBeTruthy();
  // });
});
